{
  "name": "unlib.js",
  "version": "1.0.0",
  "description": "A general library includes Random, etc.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc index.ts -t es5 --lib esnext --downlevelIteration",
    "build6": "tsc index.ts -t es6 --lib esnext"
  },
  "keywords": [
    "lib",
    "random"
  ],
  "author": "Untitled",
  "license": "MIT",
  "dependencies": {
    "@types/node": "^10.0.4"
  }
}
