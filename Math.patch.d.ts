/**
 * Once you've called `unlib.Math.patch`, you are able to use the following methods.
 */
interface Math {
  fac(num: number): number
  per(total: number, num: number): number
  com(total: number, num: number): number
}